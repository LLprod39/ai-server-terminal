<Page
    x:Class="MiniProd.Desktop.Pages.McpPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24" ColumnSpacing="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1.2*" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>

        <Border Style="{StaticResource HeroCardStyle}">
            <StackPanel Spacing="12">
                <TextBlock FontSize="20" FontWeight="SemiBold" Text="MCP servers" />
                <TextBlock
                    Foreground="{StaticResource ShellSubtleTextBrush}"
                    Text="Desktop CRUD/test/inspect flows should bind to the versioned MCP DTO endpoints."
                    TextWrapping="WrapWholeWords" />
                <ListView
                    x:Name="McpServersList"
                    DisplayMemberPath="Name"
                    SelectionChanged="OnServerSelectionChanged" />
            </StackPanel>
        </Border>

        <Border
            Grid.Column="1"
            Style="{StaticResource HeroCardStyle}">
            <StackPanel Spacing="12">
                <TextBlock FontSize="20" FontWeight="SemiBold" Text="Selected MCP server" />
                <TextBlock x:Name="McpServerNameText" FontSize="18" FontWeight="SemiBold" />
                <TextBlock x:Name="McpTransportText" />
                <TextBlock x:Name="McpHealthText" />
                <TextBlock
                    Foreground="{StaticResource ShellSubtleTextBrush}"
                    Text="Secret env values stay server-side; this client only needs masked metadata and explicit replace operations."
                    TextWrapping="WrapWholeWords" />
            </StackPanel>
        </Border>
    </Grid>
</Page>
