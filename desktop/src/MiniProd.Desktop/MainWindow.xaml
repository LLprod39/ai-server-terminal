<Window
    x:Class="MiniProd.Desktop.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{StaticResource ShellBackgroundBrush}">
        <NavigationView
            x:Name="ShellNavigationView"
            AlwaysShowHeader="False"
            Background="Transparent"
            CompactPaneLength="54"
            IsBackButtonVisible="Collapsed"
            IsPaneOpen="False"
            IsPaneToggleButtonVisible="True"
            IsSettingsVisible="True"
            OpenPaneLength="216"
            PaneBackground="{StaticResource ShellPaneBrush}"
            PaneDisplayMode="LeftCompact"
            SelectionChanged="OnNavigationSelectionChanged">

            <NavigationView.PaneHeader>
                <Border Padding="10,12,10,8">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <Border
                            Width="34"
                            Height="34"
                            Background="{StaticResource ShellPrimaryBrush}"
                            CornerRadius="10">
                            <TextBlock
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                FontFamily="{StaticResource ShellDisplayFontFamily}"
                                FontSize="14"
                                FontWeight="SemiBold"
                                Foreground="White"
                                Text="M" />
                        </Border>

                        <StackPanel Grid.Column="1" Margin="10,0,0,0" VerticalAlignment="Center">
                            <TextBlock Style="{StaticResource SectionTitleTextStyle}" Text="MiniProd" />
                            <TextBlock Style="{StaticResource CaptionTextStyle}" Text="Desktop" />
                        </StackPanel>
                    </Grid>
                </Border>
            </NavigationView.PaneHeader>

            <NavigationView.MenuItems>
                <NavigationViewItem
                    x:Name="ServersItem"
                    Content="Servers"
                    Tag="servers">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE968;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem
                    x:Name="TerminalItem"
                    Content="Terminal"
                    Tag="terminal">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE756;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.MenuItems>

            <NavigationView.PaneFooter>
                <Border Padding="10,8,10,10">
                    <TextBlock
                        Style="{StaticResource CaptionTextStyle}"
                        Text="Standalone mode"
                        HorizontalAlignment="Center" />
                </Border>
            </NavigationView.PaneFooter>

            <Frame x:Name="ContentFrame" />
        </NavigationView>
    </Grid>
</Window>
